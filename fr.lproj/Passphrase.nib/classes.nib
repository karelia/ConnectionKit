{
    IBClasses = (
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }, 
        {
            ACTIONS = {cancel = id; unlock = id; }; 
            CLASS = SSHPassphrase; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                oKeyLocation = NSTextField; 
                oPanel = NSPanel; 
                oPassword = NSSecureTextField; 
                oSave = NSButton; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}