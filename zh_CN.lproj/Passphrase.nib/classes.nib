{
    IBClasses = (
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {cancel = id; unlock = id; }; 
            CLASS = SSHPassphrase; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                oKeyLocation = NSTextField; 
                oPanel = NSPanel; 
                oPassword = NSSecureTextField; 
                oSave = NSButton; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}